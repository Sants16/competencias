import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        backgroundColor: '#121214',
        marginTop: 16,
        padding: 16,
        borderRadius: 5
    },
    descricao: {
        color: '#fff',
        fontSize: 16,
        fontWeight: '400'
    },
})